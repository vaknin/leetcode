#![allow(dead_code, unused_variables, clippy::ptr_arg, clippy::manual_memcpy, unused_mut, clippy::collapsible_if)]

fn main() {

    // let word1 = "ABABAB".to_string();
    // let word2 = "ABAB".to_string();
    // let res = gcd_of_strings(word1, word2);
    // println!("{:?}", res);
}